{
  "name": "hcg-harness-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HCG portable development harness — Claude Code plugin marketplace. Repo-level tooling (hook unit tests); the plugin itself lives in hcg-harness/.",
  "scripts": {
    "test": "node --test hcg-harness/hooks/*.test.mjs"
  },
  "optionalDependencies": {
    "js-yaml": "^4.1.0"
  }
}
